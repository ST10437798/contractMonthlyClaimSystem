<Window x:Class="WpfClaimSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClaimSystem"
        mc:Ignorable="d"
        Title="Contract Monthly Claim System" Height="600" Width="800"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#FF1E88E5"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,20,0,30"/>
        </Style>
        <Style x:Key="SubHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FF424242"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
        </Style>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#FF2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF1976D2"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#FF1565C0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Background="#FFF5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Text="Contract Monthly Claim System" Style="{StaticResource HeaderStyle}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="20,0">
            <TextBlock Text="Welcome! Please choose an option below to continue."
                       HorizontalAlignment="Center"
                       FontSize="16"
                       Margin="0,0,0,20"/>
            <TextBlock Text="Lecturer Options" Style="{StaticResource SubHeaderStyle}"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Submit Claim" Click="SubmitClaim_Click" Style="{StaticResource ButtonStyle}"/>
                <Button Content="Upload Documents" Click="UploadDocuments_Click" Style="{StaticResource ButtonStyle}"/>
                <Button Content="Track Claim Status" Click="TrackClaimStatus_Click" Style="{StaticResource ButtonStyle}"/>
            </StackPanel>
            <TextBlock Text="Coordinator / Academic Manager Options" Style="{StaticResource SubHeaderStyle}" Margin="0,30,0,10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Verify Claims" Click="VerifyClaims_Click" Style="{StaticResource ButtonStyle}"/>
                <Button Content="Approve Claims" Click="ApproveClaims_Click" Style="{StaticResource ButtonStyle}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>